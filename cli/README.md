# CLI

**Note:** This directory is deprecated. The active CLI implementation is located at `/cli.ts` in the project root.

## Active CLI Location

The ChainEquity CLI is implemented in `/cli.ts` at the project root. Run it with:

```bash
npm run cli <command> [options]
```

See the main README.md or run `npm run cli --help` for available commands.
